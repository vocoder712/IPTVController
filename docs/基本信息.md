## 拓扑
- 光猫LAN3-TPLINK路由器WAN口
- TP-LINK路由器自动获取WAN口IP地址
> IP地址192.168.1.2
> 
> 网关192.168.1.1
> 
> 子网掩码255.255.255.0
> 
> DNS服务器192.168.1.1 , 0.0.0.0
- TP-LINK路由器形成子网 192.168.0.1/24
- 子网的192.168.1.1转发到TP-LINK的192.168.1.1（光猫）

## 光猫管理后台
http://192.168.1.1
超级管理员用户名 cqadmin
密码 Cq28128717

## 光猫telnet
23端口，重启机器后会自动关闭，需要在管理网页重新开启。
普通用户名 admin
密码 123456
登录后 `su` 切换到超级管理员，密码 aDm8H%MdA

## 光猫系统概况

### CPU 与 Go 构建兼容性

- 系统报告 `armv7l`，CPU 为 ARMv7 Processor rev 1。
- `/proc/cpuinfo` 的 Features 不包含 `vfp` 或 `vfpv3`。
- Go 交叉构建必须设置 `GOARM=5`，使用软浮点兼容代码。
- `GOARM=7` 二进制虽然能编译成功，但在设备启动时会立即报告
  `Illegal instruction`。

### 文件系统
```
#df -h
Filesystem                Size      Used Available Use% Mounted on
ubi0:rootfs              41.7M     38.0M      3.7M  91% /
/dev/mtdblock2            3.0M    444.0K      2.6M  14% /tagparam
tmpfs                    80.0M     12.7M     67.3M  16% /var
tmpfs                     1.0M         0      1.0M   0% /mnt
none                     21.8M         0     21.8M   0% /run
none                      5.0M         0      5.0M   0% /run/lock
none                    109.0M         0    109.0M   0% /run/shm
/dev/mtdblock5            9.0M    612.0K      8.4M   7% /userconfig
cgroup_root             109.0M         0    109.0M   0% /sys/fs/cgroup
/dev/mtdblock13          22.0M      5.6M     16.4M  25% /preplugin
ubi1_0                   48.8M     22.0M     24.3M  47% /opt/cu/apps
overlay                  80.0M     12.7M     67.3M  16% /etc
overlay                  80.0M     12.7M     67.3M  16% /usr
overlay                  80.0M     12.7M     67.3M  16% /lib
overlay                  80.0M     12.7M     67.3M  16% /bin
overlay                  80.0M     12.7M     67.3M  16% /home/telnet
/dev/mtdblock14           2.0M    392.0K      1.6M  19% /GN25L95_datas
/dev/mtdblock7            3.0M    384.0K      2.6M  13% /wlan
overlay                  48.8M     22.0M     24.3M  47% /CuInform
tmpfs                     5.5M     20.0K      5.5M   0% /CuInform/tmp
/dev/mtdblock10           4.9M      4.9M         0 100% /opt/cu/framework
```
一般将应用程序持久放置在 `/opt/cu/apps/apps/opt/apps/` 目录下。当前已经安装了 cpolar 内网穿透，可以外网访问光猫 23 80 端口和TP-LINK路由器的管理后台。
设备存储空间极为有限，仅20M左右可用空间，不要记录大量日志。
### 启动
根文件是易失的，重启后会丢失。光猫每天晚上都会自动重启，重启后telnet会关闭。
内置一个lxc chroot子系统，子系统根目录在 `/opt/cu/framework/ufw/rootfs`。
如果要在openwrt下运行程序，请在 `/opt/cu/apps/apps/root/scripts` 目录下创建启动脚本并重启系统。
光猫开机很慢，约2分钟。

## LAN2 口可靠开关方法

### 已确认的接口映射

- LAN2 对应 Linux 接口 `eth1`。
- 应通过 LAN3 或其他管理链路登录光猫，避免关闭 LAN2 后同时中断管理会话。
- 普通用户登录 Telnet 后，需要执行 `su` 切换为超级管理员；实际开关接口需要 root 权限。

### 标准开关命令

关闭 LAN2：

```sh
ip link set dev eth1 down
```

开启 LAN2：

```sh
ip link set dev eth1 up
```

命令执行后必须读取实际链路状态，不应只根据命令退出状态判断：

```sh
ip link show dev eth1
cat /sys/class/net/eth1/operstate
cat /sys/class/net/eth1/carrier
```

正常开启且网线、对端设备均正常时，应看到：

- `ip link` 包含 `UP,LOWER_UP`，并显示 `state UP`；
- `operstate` 为 `up`；
- `carrier` 为 `1`。

关闭后应看到 `state DOWN`，且接口标志中不再包含 `UP,LOWER_UP`。

### 短时断连测试

进行短时断连时，应先启动一个独立的延迟恢复命令作为兜底，再执行正式的
`down`、等待和 `up`。下面的命令会断开 LAN2 5 秒，并在 12 秒后再次强制
开启一次 LAN2，防止控制会话异常导致端口长时间关闭：

```sh
nohup sh -c 'sleep 12; ip link set dev eth1 up' >/dev/null 2>&1 &
ip link set dev eth1 down
echo "DOWN_EPOCH=$(date +%s)"
sleep 5
ip link set dev eth1 up
echo "UP_EPOCH=$(date +%s)"
sleep 2
ip link show dev eth1
echo "OPER=$(cat /sys/class/net/eth1/operstate)"
echo "CARRIER=$(cat /sys/class/net/eth1/carrier)"
```

注意：

- 延迟恢复命令必须在 `down` 之前启动，并使用 `nohup` 脱离 Telnet 会话。
- `UP_EPOCH - DOWN_EPOCH` 应等于计划的断连秒数。
- 恢复后应同时确认 `operstate=up` 和 `carrier=1`。
- 可继续采样 `/sys/class/net/eth1/statistics/rx_bytes` 与 `tx_bytes`；计数持续
  增长表示链路恢复后已有业务流量。
- 永久关闭 LAN2 时不要使用上述延迟恢复兜底，直接使用标准关闭命令。

### 2026-07-23 实机验证记录

- 测试前：`eth1` 为 `UP,LOWER_UP`，`operstate=up`，`carrier=1`。
- 断开时间戳：`1784807042`。
- 恢复时间戳：`1784807047`。
- 实际断连时间：5 秒。
- 恢复两秒后：`operstate=up`，`carrier=1`。
- 恢复后的 5 秒内，RX 增加 4,646 bytes，TX 增加 5,401 bytes。
- 电视端观察到预期的短暂业务中断，随后链路和业务流量恢复。

本次测试期间，`192.168.1.1:8088` 可以建立 TCP 连接，但 HTTP 健康检查和
状态接口没有返回数据。因此，在该问题查明前，Telnet 登录后使用上述
`ip link` 命令是已实机验证的可靠控制方法。
